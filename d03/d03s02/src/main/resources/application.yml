logging:
  level:
    root: debug
    org:
      apache: warn
      hibernate: warn
      jboss: warn
      springframework: warn

  pattern:
    console: '%clr([%date{HH:mm:ss.SSS}]){faint} %clr(%2p) %clr(%-40.40logger{36}){cyan} %m%n%wex'
